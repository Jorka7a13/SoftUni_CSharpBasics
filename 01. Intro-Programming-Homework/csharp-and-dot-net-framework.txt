C# is a programming language. ".NET" contains the ".NET Framework" which is 
an application framework library and the "Common Language Runtime" which is 
the runtime in which .NET assemblies are run. 